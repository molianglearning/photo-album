# ==========================================
# 私密相册系统 - 环境配置文件
# ==========================================
# 复制此文件为 .env 并修改配置
# 生产环境部署时务必修改所有密码和密钥！
# ==========================================

# 运行环境
# development: 开发环境
# production: 生产环境（部署时必须设置为 production）
NODE_ENV=production
PORT=3000

# ==========================================
# 数据库配置
# ==========================================
# PostgreSQL 配置（推荐）
DB_HOST=localhost
DB_PORT=5432
DB_NAME=photo_album_db
DB_USER=photo_user
DB_PASSWORD=your_database_password_change_this

# MySQL 配置（如使用 MySQL，需修改 server/config/database.js）
# DB_HOST=localhost
# DB_PORT=3306
# DB_NAME=photo_album_db
# DB_USER=photo_user
# DB_PASSWORD=your_database_password_change_this

# ==========================================
# JWT 安全配置
# ==========================================
# JWT_SECRET: 用于加密用户令牌，必须修改为随机字符串！
# 生成方法: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_random_secret_key_change_this_in_production_12345
JWT_EXPIRE=7d

# ==========================================
# 文件上传配置
# ==========================================
UPLOAD_PATH=./server/uploads
MAX_FILE_SIZE=10485760

# ==========================================
# 超级密码（紧急访问通道）
# ==========================================
# 可用于前台和后台登录，即使忘记常规密码也能使用
# ⚠️ 生产环境部署后请务必修改为复杂密码！
# 建议: 至少 12 位，包含大小写字母、数字和特殊字符
SUPER_PASSWORD=SuperAdmin@2024
