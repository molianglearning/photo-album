# 长按拖拽排序使用指南

## ✨ 新功能：长按拖拽

现在支持更直观的拖拽方式，特别适合移动端！

## 🎯 如何使用

### 桌面端（电脑）

1. **点击并按住**列表项任意位置
2. 等待 **0.2秒**（会看到项目放大并高亮）
3. **拖动**到目标位置
4. **松开鼠标**完成排序

### 移动端（手机/平板）

1. **长按**列表项任意位置
2. 等待 **0.2秒**（会看到项目放大并高亮）
3. **拖动**到目标位置
4. **松开手指**完成排序

## 📱 视觉反馈

### 1. 长按选中（0.2秒后）
- 背景变为**浅蓝色** (#e3f2fd)
- 项目**放大 5%**
- 出现**蓝色阴影**
- 提示：可以开始拖动了

### 2. 拖动中
- 被拖动的项目：
  - 白色背景
  - 深色阴影
  - 轻微旋转（2度）
  - 跟随手指/鼠标移动

- 原位置占位符：
  - 灰色背景
  - 蓝色虚线边框
  - 半透明显示

### 3. 松开后
- 项目移动到新位置
- 自动保存到服务器
- 恢复正常样式

## 🎨 完整流程演示

```
1. 正常状态
   ┌─────────────────┐
   │ 分类 1          │
   ├─────────────────┤
   │ 分类 2          │  ← 想移动这个
   ├─────────────────┤
   │ 分类 3          │
   └─────────────────┘

2. 长按 0.2秒（选中）
   ┌─────────────────┐
   │ 分类 1          │
   ├─────────────────┤
   │ 分类 2          │  ← 放大+蓝色高亮
   ├─────────────────┤
   │ 分类 3          │
   └─────────────────┘

3. 拖动中
   ┌─────────────────┐
   │ 分类 1          │
   ├─────────────────┤
   │ [占位符]        │  ← 虚线框
   ├─────────────────┤
   │ 分类 3          │
   └─────────────────┘
        ↓
   ┌─────────────┐
   │ 分类 2      │  ← 跟随移动
   └─────────────┘

4. 松开（完成）
   ┌─────────────────┐
   │ 分类 1          │
   ├─────────────────┤
   │ 分类 3          │
   ├─────────────────┤
   │ 分类 2          │  ← 移动到这里
   └─────────────────┘
```

## ⚙️ 技术参数

| 参数 | 值 | 说明 |
|------|-----|------|
| delay | 200ms | 长按延迟时间 |
| delayOnTouchOnly | true | 只在触摸设备启用延迟 |
| animation | 200ms | 动画过渡时间 |
| swapThreshold | 0.65 | 交换阈值 |
| forceFallback | true | 强制使用回退模式 |

## 💡 使用技巧

### 1. 桌面端
- 可以直接点击拖动（无需等待）
- 也可以长按后拖动（更稳定）
- 推荐：直接拖动

### 2. 移动端
- 必须长按 0.2秒
- 看到高亮后再移动
- 推荐：长按等待反馈

### 3. 快速排序
- 一次只移动一个项目
- 可以连续操作
- 每次都会自动保存

### 4. 取消拖动
- 松开前移回原位置
- 或者点击其他地方
- 不会保存更改

## 🐛 常见问题

### Q1: 长按后没有反应？

**检查：**
1. 是否等待了 0.2秒？
2. 浏览器控制台是否有错误？
3. 是否看到"拖拽排序初始化完成"日志？

**解决：**
```bash
# 重启服务器
npm run dev

# 硬刷新浏览器
Ctrl + Shift + R
```

### Q2: 拖动时项目消失了？

**原因：** 样式冲突

**解决：**
- 检查是否有其他 CSS 覆盖
- 查看浏览器控制台的样式

### Q3: 移动端拖不动？

**原因：** 触摸事件冲突

**解决：**
- 确保没有其他触摸事件监听
- 尝试在不同浏览器测试
- 检查是否启用了 `delayOnTouchOnly`

### Q4: 排序没有保存？

**检查：**
1. 网络请求是否成功
2. 控制台是否显示"排序保存成功"
3. 刷新页面查看是否保持

**解决：**
- 检查网络连接
- 查看服务器日志
- 确认 API 正常工作

## 🔍 调试方法

### 1. 查看控制台日志

打开浏览器控制台（F12），应该看到：

```
拖拽排序初始化完成
选中项目: 1
开始拖拽: 1
拖拽结束: 1 -> 3
排序保存成功
```

### 2. 检查网络请求

在 Network 标签查找：
- `/api/admin/categories/sort`
- `/api/admin/albums/sort`

### 3. 测试不同场景

- [ ] 桌面端 Chrome
- [ ] 桌面端 Firefox
- [ ] 移动端 Safari
- [ ] 移动端 Chrome
- [ ] 平板设备

## 📊 性能优化

### 1. 列表项数量
- 推荐：< 50 个
- 最大：< 100 个
- 超过会影响性能

### 2. 动画流畅度
- 关闭其他标签页
- 使用硬件加速
- 避免同时运行其他动画

### 3. 移动端优化
- 使用原生滚动
- 避免过度动画
- 减少 DOM 操作

## 🎯 最佳实践

### 1. 用户引导
- 页面顶部显示提示："💡 长按列表项可拖拽排序"
- 首次使用时可以添加引导动画
- 提供帮助文档链接

### 2. 错误处理
- 拖拽失败时显示提示
- 自动回滚到原位置
- 提供重试选项

### 3. 反馈机制
- 选中时立即反馈
- 拖动时实时更新
- 完成时显示成功提示

## 🔄 与旧版本对比

| 特性 | 旧版本 | 新版本 |
|------|--------|--------|
| 拖拽触发 | 点击小图标 | 长按任意位置 |
| 图标大小 | 20px | 24px |
| 延迟时间 | 无 | 200ms |
| 视觉反馈 | 简单 | 丰富 |
| 移动端 | 一般 | 优秀 |
| 桌面端 | 良好 | 优秀 |

## 📝 更新日志

### v2.0 - 长按拖拽
- ✅ 添加长按延迟（200ms）
- ✅ 优化视觉反馈
- ✅ 增强移动端体验
- ✅ 添加使用提示
- ✅ 改进动画效果

### v1.0 - 基础拖拽
- ✅ 基础拖拽功能
- ✅ 小图标触发
- ✅ 简单视觉反馈

## 🚀 未来计划

- [ ] 添加触觉反馈（震动）
- [ ] 支持批量移动
- [ ] 添加撤销功能
- [ ] 支持键盘操作
- [ ] 添加拖拽音效

## 💬 反馈

如果有任何问题或建议，请：
1. 查看控制台日志
2. 截图错误信息
3. 描述操作步骤
4. 提供设备信息
