# ==========================================
# Vercel 环境变量配置模板
# ==========================================
# 复制以下变量到 Vercel 项目设置中

# 运行环境
NODE_ENV=production

# 数据库配置（Vercel 临时存储）
DB_PATH=/tmp/database.sqlite

# JWT 安全配置（请生成新的随机密钥）
# 生成命令: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=请替换为随机生成的密钥
JWT_EXPIRE=7d

# 文件上传配置（Vercel 临时存储）
UPLOAD_PATH=/tmp/uploads
MAX_FILE_SIZE=10485760

# 超级密码（请修改为强密码）
SUPER_PASSWORD=请修改为你的强密码

# ==========================================
# 可选配置
# ==========================================
# 如果需要外部数据库，可以添加：
# DATABASE_URL=你的外部数据库连接字符串

# 如果需要云存储，可以添加：
# CLOUDINARY_URL=你的Cloudinary连接字符串
# AWS_ACCESS_KEY_ID=你的AWS访问密钥
# AWS_SECRET_ACCESS_KEY=你的AWS密钥